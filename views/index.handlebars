<h1>Burger Munch</h1>

<h2>Devoured burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/bur-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/bur-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add new burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="nb" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Have you eaten yet</label><br>
    <input type="radio" name="sleepy" value="1" eaten> DEVOURED!<br>
    <input type="radio" name="sleepy" value="0"> gotta try
  </div>

  <button type="submit">Add burger</button>
</form>
